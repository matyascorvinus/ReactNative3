import React, { Component } from 'react';
import {
  Text,
  View,
} from 'react-native';
import SectionScreen from './SectionScreen';

class App extends Component {
  state = {  }
  render() {
    return (
      <SectionScreen/>
    );
  }
}

export default App;